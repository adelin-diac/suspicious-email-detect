/**
 * Checks if an email address is suspicious based on its domain
 * @param email - The email address to check
 * @returns Returns true if the email is suspicious, false otherwise
 */
export function isSuspicious(email: string): boolean;
